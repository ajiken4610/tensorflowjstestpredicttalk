import * as tf from "@tensorflow/tfjs";
// import genData from "utils/genData";
addEventListener("message", async (e) => {
  const { trainSequence, batchSize, maxLength, testSequence, model } = e.data;
});
